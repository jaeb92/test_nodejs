<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partial/head') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        $(document).on('click', '#btnSave', function(e) {
            var title = $('#title').val();
            var content = $('#content').val();
            var writer = $('#writer').val();
            if((title.length == 0 && title == '') || (content.length == 0 && content == '') ||
                    (writer.length == 0 && writer == '')) {
                alert("제목, 내용, 작성자를 입력하세요");
            } else {
                e.preventDefault();
                $("form").submit();	
            }
        });

    </script>
</head>
<%- include('../partial/nav') %>
<body>
	<article>
		<div class="container" role="main">
            <form action="/post/write" method="post">
		    	<div class="mb-3">
					<label for="title">제목</label> 
					<input name="posttitle" type="text" class="form-control" id="title" placeholder="제목을 입력해주세요" />
				</div>
				<div class="mb-3">
					<label for="reg_id">작성자</label> 
					<input name="postwriter" type="text" class="form-control" id="writer" placeholder="작성자를 입력하세요"/>
				</div>
				<div class="mb-3">
					<label for="content">내용</label>
					<textarea name="postcontent" class="form-control" rows="20" id="content"
						placeholder="내용을 입력해 주세요" ></textarea>
				</div>
                <div>
                    <button type="button" class="btn btn-sm btn-primary" id="btnSave">저장</button>
                    <button type="button" class="btn btn-sm btn-primary" onclick="location.href='/post/list'">목록</button>
                </div>
			</form>
		</div>
	</article>
</body>
</html>
</html>